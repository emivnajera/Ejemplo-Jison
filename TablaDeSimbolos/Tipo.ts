export enum TIPO{
    ENTERO = 1,
    DECIMAL = 2,
    BOOLEANO = 3,
    CARACTER = 4,
    CADENA = 5,
}

export  enum  OperadorAritmetico{
    MAS = 1,
    MENOS = 2,
    POR = 3,
    DIV = 4,
    POT = 5,
    MOD = 7,
    UMENOS = 8,
}

export enum OperadorRelacional{
    MENORQUE = 1,
    MAYORQUE = 2,
    MENORIGUAL = 3,
    MAYORIGUAL = 4,
    IGUALIGUAL = 5,
    DIFERENTE = 6,
}

export enum OperadorLogico{
    NOT = 1,
    AND = 2,
    OR =3,
}